## Goal

Publish the supplied Tamil news report as a new article dated today, with the attached altar/portrait photo as the lead image.

## Article metadata I'll use

- **Title**: `கம்போங் காசிப்பிள்ளை RT முன்னோடியின் மகன் கார்த்திக்கு துணையமைச்சர் யுனேஸ்வரன் ஆறுதல்`
- **Category**: தேசியம் (national unity / Rukun Tetangga heritage fits best)
- **Source**: none (original editorial, not from an RSS feed)
- **Status**: `published`, `publish_date = now()` (Asia/Kuala_Lumpur)
- **is_breaking**: false. **is_featured**: leave to the auto-feature trigger.
- **Excerpt**: first paragraph trimmed (~155 chars) for SEO meta.
- **Content**: full body you supplied, paragraphs preserved, with the two subheadings (`ஒற்றுமையின் வரலாற்று மரபு`, `அடிப்படைத் தலைவர்களுக்கு அங்கீகாரம்`) kept as inline headings.
- **Lead image**: photo 1 (the garlanded portrait at the prayer altar) — most fitting for a condolence story.
- Slug auto-generated by the existing `article_slug_trigger`.

## Steps

1. **Create a public storage bucket `article-images**` via migration (none exists yet). Policies: public read; admin-only insert/update/delete using the existing `is_admin(auth.uid())` helper.
2. **Copy both uploaded photos** from `user-uploads://` into `/tmp/` so they can be uploaded:
  - `703337088_...jpg` → altar / portrait (lead image)
  - `703335558_...jpg` → deputy minister with family (kept as secondary, only the lead is referenced in `image_url`; let me know if you also want it embedded inline in the body)
3. **Upload both images** to `article-images/editorial/2026-05-21/...jpg` via the storage upload tool → get public URLs.
4. **Insert the article row** with the metadata above. Slug, `is_featured`, and `updated_at` are handled by triggers.
5. **Verify**: `SELECT slug, title, publish_date, status FROM articles ORDER BY created_at DESC LIMIT 1;` and confirm the article appears on the homepage at `/`.

## Questions before I proceed

1.  **embed photo 2 inline in the body too?** 
2. தேசியம் 

If you reply "go" I'll use the defaults (தேசியம் + photo 1 lead only) and ship it.